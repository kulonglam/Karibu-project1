ReferenceError: logout is not defined. The function logout was missing from the file, but addEventListener tried to reference it.	
I re-added the function logout to the script scope so it exists when the button tries to call it.
Format the code using ESLint and Prettier.
